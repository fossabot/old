<template>
    <div class="detail selectable khmerHanuman">
        <h1 v-if="detail[0].t_subword && detail[0].t_subword!='' && detail[0].t_subword!=null && detail[0].t_subword!=undefined">{{ detail[0].t_subword }}</h1>
        <h1 v-else>{{ detail[0].t_main }} <span v-if="detail[0].t_subscrip" class="subcript">{{ getKhmerNumber(detail[0].t_subscrip) }}</span></h1>
        <template v-if="detail[0].t_pron">
            <p>{{ detail[0].t_pron }}</p>
        </template>
        <template v-if="detail[0].t_source">
            <p>{{ detail[0].t_source }}</p>
        </template>
        <template v-for="item in detail" :key="item.id">
            <br/>
            <span>{{ item.t_poly }}</span>
            <template v-if="item.t_pos">
                <span class="d-part-of-speech">{{ item.t_pos }}</span>
            </template>
            <template v-if="item.t_exp">
                <span class="d-exp">{{ item.t_exp }}</span>
            </template>
            <template v-if="item.t_exam">
                <span class="d-example">{{ item.t_exam }}</span>
            </template>
            <template v-if="item.t_notes">
                <span class="d-note">{{ item.t_notes }}</span>
            </template>
        </template>
    </div>
</template>

<script>
import DictionaryController from '../../../db/DictionaryController'
export default {
    name: 'KhmerDictionaryDetail',
    props: ['detail'],
    data: () => ({
        
    }),
    methods: {
        getKhmerNumber: function(item){
            return DictionaryController.getKhmerNumber(item)
        }
    }
}
</script>

<style scoped>
    p,span{
        white-space: pre-line;
        font-size: 20px;
        text-align: start;
        line-height: 2em;
    }

    .detail{
        display: block;
        /* flex-direction: column; */
        align-items: flex-start;
        width: 100%;
        height: 100%;
        padding: 15px;
        overflow-y: auto;
        text-align: start;
    }

    .d-part-of-speech{
        color: red;
        padding-left: 5px;
    }
    .d-exp{
        padding-left: 5px;
    }

    .d-example{
        color: rgb(var(--v-theme-example));
        padding-left: 5px;
    }
    .d-note{
        padding-left: 5px;
    }
</style>