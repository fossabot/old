<template>
    <div class="detail selectable khmerHanuman">
        <h1>{{ detail[0].writtenForm }}</h1>
        <template v-if="detail[0].pronunciation">
            <p>({{ detail[0].pronunciation }})</p>
        </template>
        <template v-for="item in detail" :key="item.id">
            <br/>
            <template v-if="item.partOfSpeech">
                <span class="d-part-of-speech">{{ getPartOfSpeech(item) }}</span>
            </template>
            <template v-if="item.definition">
                <span>{{ item.definition }}</span>
            </template>
            <template v-if="item.example">
                <span class="d-example">{{ item.example }}</span>
            </template>
        </template>
    </div>
</template>

<script>
import DictionaryController from '../../../db/DictionaryController'
export default {
    name: 'ChounnatDetail',
    props: ['detail'],
    data: () => ({
        
    }),
    methods: {
        getPartOfSpeech(item){
          return  DictionaryController.getFullPathOfSpeech(item.partOfSpeech)
        }
    }
}
</script>

<style scoped>
    p,span{
        white-space: pre-line;
        font-size: 20px;
        text-align: start;
        line-height: 2em;
    }

    .detail{
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        width: 100%;
        height: 100%;
        padding: 15px;
        overflow-y: auto;
    }

    .d-part-of-speech{
        color: red;
    }

    .d-example{
        color: rgb(var(--v-theme-example));
    }
</style>